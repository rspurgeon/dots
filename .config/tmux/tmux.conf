# tmux 3.6 loads ~/.config/tmux/tmux.conf after ~/.tmux.conf on this host.
# Keep the canonical gpakosz + dots local config last instead of letting
# Omarchy's XDG tmux defaults override it.
source-file ~/.tmux.conf
